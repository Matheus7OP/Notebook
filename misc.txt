// otimizações do compilador
#pragma GCC target("sse4")
#pragma GCC optimize("Ofast")

// otimizações do I/O do C++
ios::sync_with_stdio(false); // lembrar de não usar I/O de C, pois estará dessincronizado do I/O de C++
cin.tie(null) // possível usar em problemas não interativos
